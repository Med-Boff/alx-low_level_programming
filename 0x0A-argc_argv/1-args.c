#include <stdio.h>

/**
 * main - prints the number of caracters given to this program
 * @argc: stores the num of argments
 * @argv: stores the arguments
 *
 * Return: always 0
 */

int main(int argc, char *argv[])
{
	(void)*argv;

	printf("%d\n", argc);
	retueswfw4ern (0);
}
